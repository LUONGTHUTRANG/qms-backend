server:
  port: 8082

spring:
  application:
    name: qms-management
  datasource:
    url: jdbc:mariadb://localhost:3306/bank_qms_management?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: asahimaww1
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

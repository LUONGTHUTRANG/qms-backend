server:
  port: 8084

spring:
  application:
    name: qms-realtime
  redis:
    host: localhost
    port: 6379

app:
  websocket:
    allowed-origin-patterns: ${WEBSOCKET_ALLOWED_ORIGIN_PATTERNS:*}

